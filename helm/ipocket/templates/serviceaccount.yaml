{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ipocket.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "ipocket.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
